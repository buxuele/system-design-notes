# 站点开发维护手册

面向日常修改正文、加图、加章节、跑测试的完整操作说明。

## 一、项目结构

1. Readme.md，项目说明与 28 章目录。
2. 系统设计笔记，中文正文唯一来源，也是 VitePress 的 srcDir。
3. .vitepress/config.mts，站点配置，自动扫描章节目录生成重写规则与侧边栏。
4. vercel.json，Vercel 构建参数，固化构建命令与输出目录。
5. translate_notes.py，翻译脚本，参数与流程见 中文翻译说明.md。
6. tests，六个测试文件，交付前必须全量跑一遍。
7. .env，本地 OpenRouter 密钥，已 gitignore，永不入库。
8. 根目录下的 28 个英文原版章节目录，已 gitignore，仅供翻译脚本比对，不随仓库发布。
9. 文档六份：Readme.md、中文翻译说明.md、部署到Vercel指南.md、站点检查报告.md、站点开发维护手册.md、仓库迁移记录.md。

## 二、路由与侧边栏规则

1. 系统设计笔记/README.md 输出为首页 /。
2. 章节目录名去掉数字前缀后转小写，非字母数字全部转为连字符，得到 slug。例如 04. Rate Limiter 输出为 /04-rate-limiter/。
3. config.mts 启动时扫描章节目录，自动生成 rewrites 与侧边栏，新增章节无需改配置。
4. 侧边栏文字取该章 README 的第一个一级标题，改标题即改侧边栏。
5. cleanUrls 已开启，地址不带 .html 后缀。
6. 正文里的站内链接必须写绝对路径，形如 /04-rate-limiter/。相对路径不会被重写改写，会指向错误位置。

## 三、日常修改流程

1. 改正文：直接编辑该章 README.md。
2. 加图片：图片放进该章 images 目录，正文用 ./images/文件名 引用，构建时自动指纹打包。
3. 新增章节：在 系统设计笔记 下建 NN. 章节名 目录并放入 README.md，配置自动接入。
4. 本地验证：npm run build 构建，跑第四节测试，npm run preview 打开 4321 端口抽查。
5. 提交推送：git push 到 main 分支，Vercel 自动触发构建部署。

## 四、测试清单

1. python3 -m pytest tests/ -q，翻译单测、粘连回归、产物嵌套校验，共 21 个用例。
2. python3 tests/test_html_balance.py，全部产物 HTML 标签平衡校验。
3. node tests/test_vue_template_parse.mjs，全部产物按 Vue 模板解析校验。
4. python3 tests/test_dist_links.py，全站内链与图片引用死链校验。
5. 四项全部通过才算构建合格，任何一项失败先看报错里的文件与行号。

## 五、内容红线

1. 代码围栏的闭合行后面粘了文字，其后整段正文会被渲染成一个大代码块。
2. 表格数据行的单元格多于表头列数时，多出的单元格内容会被静默丢弃，正文直接消失。
3. 表格与后续段落之间必须空行，否则段落会被并进表格。
4. 缩进的 HTML 图片段落紧跟列表项正文且中间无空行时，会被并进段落造成 p 套 p，开发模式报水合警告。
5. 以上问题已由 tests/test_markdown_glue.py 与 tests/test_dist_nesting.py 拦截，修改表格、代码块或列表内嵌 HTML 后必须跑 pytest。

## 六、故障排查

1. 页面 404：先确认地址是否为 /NN-slug/ 规范路径，再看构建日志里的 rewrites 输出。
2. 图片不显示：确认引用路径以 ./images/ 开头且文件确实放在该章 images 目录。
3. 侧边栏标题不对：改该章 README 的第一个一级标题，不改 config.mts。
4. 构建产物异常：删除 .vitepress/cache 后重新 npm run build。
5. 判断本地服务是否正常：curl -s --noproxy 127.0.0.1 -o /dev/null -w 输出码 http://127.0.0.1:4321/ 。环境变量与系统代理会干扰本地请求，排查时一律加 --noproxy 127.0.0.1。

## 七、仓库关系与上游同步

1. 本仓库是 liquidslr/system-design-notes 的 fork，GitHub 页面顶部显示 forked from。
2. 日常推送执行 git push origin main 即可。
3. 查看上游新提交：先执行 git fetch upstream，再执行 git log --oneline main..upstream/main。
4. 上游是英文原文，正文合并必然冲突，只同步许可证、图片与结构性变更，冲突一律以中文译文为准。
5. 远程 legacy 指向迁移前的独立仓库 buxuele/system-design-notes-legacy，本地 cn-legacy 分支保留迁移前历史，两者仅作备份，不需要推送。
