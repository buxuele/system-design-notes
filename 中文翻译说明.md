# 系统设计笔记中文化说明

## 项目简介
1. 翻译脚本为 translate_notes.py，位于工作区根目录。
2. 源目录为 system-design-notes，共 28 章英文笔记加一个根 README。
3. 输出目录为 系统设计笔记，原英文仓库不做任何改动。
4. LLM 走 OpenRouter，首选 inclusionai/ling-3.0-flash-fin:free，备用 qwen、nemotron-super、nemotron-lightning 依次降级。

## 使用步骤
1. 在工作区根目录执行 python3 translate_notes.py。
2. 已翻译的章节自动跳过，中断后再次执行即可续跑。
3. 删除 系统设计笔记 下对应章节的 README.md 即可强制重译该章。

## 质量保障
1. 关闭 reasoning，实测响应从超截断风险降到 1.4 秒稳定返回。
2. 每块校验代码块、img、链接、标题数量，并检测标题粘连，失败自动换模型重试，共 3 轮 4 模型。
3. 分块拼接用 fit_boundary 按原文补齐首尾空白，防止标题被粘进上一段。
4. 整文件再过一道标题总数硬闸门，不一致直接报错退出，不写出坏文件。
5. 图片目录整体复制，共 392 张图，根 README 的 28 条章节链接逐条验证无死链。

## 测试
1. 测试文件为 tests/test_translate_notes.py，共 18 个用例。
2. 执行 python3 -m pytest tests/ -q，全部通过。
3. 最终全量校验问题数为 0，img 387、代码块 116、表格 148、各级标题与源文件逐一对齐。
