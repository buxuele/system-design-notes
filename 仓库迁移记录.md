# 仓库迁移记录

记录本次把独立仓库迁移为原仓库 fork 的完整过程、验证结果与后续工作流。

## 一、迁移目标

1. 把 buxuele/system-design-notes 从独立仓库改为原仓库 liquidslr/system-design-notes 的真 fork。
2. 仓库 URL 保持不变，中文内容与站点功能不受任何影响。
3. 迁移前的全部提交历史完整保留，可随时找回。

## 二、迁移步骤

1. 先用 GitHub API 探测凭据与现状，确认 token 有效、旧仓库具备管理员权限、fork 字段为 false、账号下没有任何已有 fork。
2. 调用改名接口把旧仓库改为 buxuele/system-design-notes-legacy，返回 200，腾出原名。
3. 调用 fork 接口从原仓库创建 fork，返回 202，新仓库 id 为 1386296331，fork 为 true，parent 为 liquidslr/system-design-notes。
4. 本地 git fetch origin 拉取 fork 的 main，即上游最新提交 9d83887。
5. 用 git commit-tree 生成合并提交，树完全等于中文版内容，两个父节点分别为上游 main 与迁移前 main，工作区文件一字未动。
6. 更新本地 main 后推送，从 9d83887 快进到 1bf6a2a，无需强制推送。
7. 补充文档提交 1058cb3 并推送。

## 三、验证结果

1. GitHub API 返回 fork 为 true，parent 正确，default branch 为 main。
2. 本地 main 与远端 main 完全一致，均为 1058cb3。
3. 首父历史依次为 1058cb3 文档提交、1bf6a2a 合并提交、9d83887 上游提交。
4. git merge-base 校验确认新历史同时包含上游历史与迁移前历史。
5. 测试 pytest 21 passed，站内 1548 条引用零死链。
6. 实测 git fetch upstream 与 git log main..upstream/main，当前与上游没有落差。

## 四、备份与找回

1. 远程备份仓库为 buxuele/system-design-notes-legacy，保留迁移前全部提交，本地已配置 legacy 远程。
2. 本地 cn-legacy 分支指向迁移前的 main。
3. 找回迁移前状态执行 git checkout cn-legacy，或从 legacy 远程拉取对应分支。

## 五、后续工作流

1. 日常改动照旧提交，执行 git push origin main。
2. 上游同步先执行 git fetch upstream，再执行 git merge upstream/main，英文正文冲突一律以中文译文为准。
3. 完整同步说明见 站点开发维护手册.md 第七节。

## 六、影响与注意事项

1. 仓库 URL 不变，README 中的仓库地址无需修改。
2. 仓库 id 由 1383627373 变为 1386296331，若 Vercel 已导入旧仓库需要重新导入一次同名仓库。
3. GitHub 页面顶部现在显示 forked from liquidslr/system-design-notes。
4. fork 接口、改名接口、探测接口均为幂等可重入，重跑前先 GET 查询状态再决定是否执行。
