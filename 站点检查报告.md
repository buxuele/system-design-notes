# 系统设计笔记站点检查报告

## 一、检查方式

1. 本地预览站点，用 Chrome 实测首页、章节页、搜索、深色浅色主题、移动端视口与 404 页。
2. 用 Lighthouse 对首页与章节页做无障碍、最佳实践、SEO 审计。
3. 把 28 章中英文源码做结构对比，核对图片、表格、代码围栏、各级标题数量是否一致。
4. 用 pytest 与 node 脚本跑站内链接、HTML 闭合、Vue 模板解析全量测试。
5. 截图与审计报告存放在 tests/站点截图 目录。

## 二、已修复的问题

1. 第22章酒店预订系统第211行闭合围栏粘连了正文，导致其后约5426字符的估算分析与并发问题被渲染成一个大代码块。已拆分为独立段落，重建后并发问题恢复为正常三级标题。
2. 第20章指标监控第242行，段落被粘在表格行尾，多出的单元格被 markdown-it 静默丢弃，页面丢失没有明确的赢家一整段。已还原为独立段落。
3. 第21章广告点击第122行，同样粘连导致 filter_id 帮助我们实现过滤需求一整句从页面消失。已还原。
4. 第16章邻近服务第241行表格尾巴粘连了水平分隔线标记。已还原为表格加独立分隔线。
5. 首页标题层级错误，附加资源与主标题同为一级且其下直接跳到三级标题，Lighthouse 报 heading-order 失败。已改为二级标题，首页无障碍从98分提升到100分。
6. 第2、6、8章一级标题使用中文数字，与其余25章不一致，侧边栏与上下章导航显示第二章、第六章、第八章。已统一为第2章、第6章、第8章。
7. 新增 tests/test_markdown_glue.py，对表格超列丢正文与围栏粘连两类问题做永久回归拦截。

## 三、实测通过的功能

1. 28 章目录、侧边栏、上下章导航、锚点深链均正常，30 页1548条站内引用零死链。
2. 本地搜索正常，快捷键 / 与 ⌘K 均已注册，搜索结果带章节上下文。
3. 深色浅色主题切换正常且刷新后记忆正常。
4. 移动端390像素视口无横向溢出，汉堡菜单正常。
5. 404 页与图片资源响应正常。
6. 测试结果：pytest 20 passed，HTML 平衡29文件零错误，Vue 模板解析29文件零失败，站内链接零死链。
7. 修复后复测 Lighthouse：首页与章节页的无障碍、最佳实践、SEO 均为100分。

## 四、仍存在的问题与改进建议

### P1 建议尽快处理

1. 侧边栏标题与目录页标题不一致。侧边栏取各章一级标题，目录页用另一套措辞，至少9章文字不同，例如第7章在分布式系统中设计唯一ID生成器与第7章设计分布式系统中的唯一ID生成器、第28章证券交易所与第28章股票交易所、第18章Google地图与第18章设计Google地图。建议以各章一级标题为准统一目录页链接文字。
2. 404 页面是英文的 PAGE NOT FOUND，搜索按钮占位符是 Search，代码块复制按钮是 Copy Code。建议给主题补中文文案。
3. 首页主标题整行是外链，渲染成大号蓝色下划线样式，喧宾夺主。建议主标题改为纯文字，链接移到正文。
4. 第27章两个表格首列表头是空单元格，Lighthouse 移动端报 td-has-header 未通过。建议给首列表头补对比项一类的文字。
5. 全站112个代码围栏没有语言标签，代码块无语法高亮且语言名为空。建议按内容补 sql、java、python 等标签。

### P2 可以排期处理

1. 图片总重79MB，构建产物83MB，最大单图3MB在第18章。建议压缩 PNG 或转 WebP，并给正文图片加 loading lazy。
2. 站点没有 og 分享标签与 sitemap。建议在 config.mts 的 head 补 og:title、og:description、og:image，绑定域名后开启 sitemap。
3. 部分标题写成星号粗体，锚点无障碍标签会暴露星号。建议清理各级标题里的星号。
4. 首页右侧本页目录只有附加资源一组条目，信息偏少。可把各章目录纳入大纲或调整大纲级别。
5. 搜索结果把第8章的限流器一节排在第4章限流器正文之前，属本地搜索默认打分，可后续再调。

### 已知无害项

1. Lighthouse 唯一残留失败项是 label-content-name-mismatch，来自 VitePress 搜索按钮的 ⌘K 键位提示，属上游默认行为，不影响使用。

## 五、下一步

1. 确认第四节 P1 五项后逐项修复，每修一项跑一遍全量测试。
2. Vercel 导入仓库时构建命令 npm run build，输出目录 .vitepress/dist。
3. 部署后抽查首页、第4章、第27章与章节配图是否正常。
